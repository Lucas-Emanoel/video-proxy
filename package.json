{
  "name": "video-proxy",
  "version": "1.0.0",
  "description": "Proxy de vídeo para resolver problemas de CORS",
  "main": "server.js",  
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Build complete'",
    "start": "node server.js"  
  },
  "dependencies": {
    "node-fetch": "^2.6.7",
    "express": "^4.17.1"
  },
  "engines": {
    "node": ">=14.x"
  }
}
